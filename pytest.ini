[pytest]
markers =
    integration: requires a live mGBA instance on localhost:8888
    unit: fast tests with no external dependencies
    slow: long-running regression/parity tests; opt-in with -m slow
