# Large C++ self-regression manifest corpus (~1GB, 1M games) — kept locally, never committed.
# Committed gate subset: tests/fixtures/cpp_manifest/ (first 2k of the same seed sequence).
cpp_manifest_1m/

# Build outputs
cmake-build-*/
engine/build/
*.so

# Python
.venv/
__pycache__/
*.pyc

# IDE
.idea/

# Runtime logs
logs/

# mGBA savestates — ROM memory dumps, never committed.
# Recreate your own with SCRIPTS/play.py (M key) against your own copy of the game.
States/
